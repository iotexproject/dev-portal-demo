const styles = {
  global: (theme: any) => ({
      body: {
        color: "default",
      bg: "bg-canvas",
        overflowX: 'hidden'
      },
      "*::placeholder": {
        opacity: 1,
        color: "muted",
      },
  })
};

export default styles;
